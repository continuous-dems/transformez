[build-system]
requires = ["hatchling>=1.5.0", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = 'transformez'
description = 'Global vertical coordinate transformations for geodetic, tidal, and model-based references.'
readme = "README.md"
requires-python = ">=3.12"
license = { file = "LICENSE" }
dynamic = ["version"]
authors = [
    {name = "Matthew Love", email = "matthew.love@colorado.edu"},
    {name = "Christopher Amante", email = "christopher.amante@colorado.edu"},
    {name = "Elliot Lim", email = "elliot.lim@colorado.edu"},
    {name = "Michael MacFerrin", email = "michael.macferrin@colorado.edu"},
    {name = "Matt Fisher", email = "mfisher87@gmail.com"},
]
maintainers = [
    {name = "Matthew Love", email = "matthew.love@colorado.edu"},
]
classifiers = [
    'Programming Language :: Python :: 3',
    'Operating System :: OS Independent',
    'Topic :: Scientific/Engineering :: GIS',
]
dependencies = [
    'numpy>1.24',
    'pyproj>3.6.1',
    'rasterio>1.4.0',
    'fetchez>=0.8.7',
    'scipy',
    'click',
]

keywords = ["Geospatial"]

[project.optional-dependencies]
preview = ["matplotlib"]

full = ["transformez[preview]"]

[dependency-groups]
dev = [
    "pre-commit",
    "mypy",
]
test = [
    "pytest",
    "pytest-cov",
]
docs = [
    "sphinx<9.0.0",  # temp pin for sphinx-click
    "sphinx-autodoc-typehints>=2.0.1",
    "sphinx-click>=6.0",
    "pydata-sphinx-theme",
    "myst-parser",
    "sphinxcontrib-mermaid",
]

[project.entry-points."fetchez.modules"]
transformez_modules = "transformez.integrations.fetchez.modules"

[project.scripts]
transformez = 'transformez.cli:transformez_cli'

[project.urls]
Homepage = "https://github.com/continuous-dems/transformez"
Issues = "https://github.com/continuous-dems/transformez/issues"

# --- Build configuration ---

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.version]
path = "src/transformez/_version.py"

[tool.hatch.build.targets.wheel]
packages = ["src/transformez"]

[tool.hatch.build.targets.sdist]
include = [
  "src/transformez",
  "tests",
  "CHANGELOG.md",
  "CITATION.cff",
  "CONTRIBUTING.md",
  "AUTHORS.md",
]
exclude = [
  ".github",
]

# --- Devtools configuration ---

[tool.mypy]
mypy_path = "src"
packages = ["transformez"]
python_version = "3.12"
disable_error_code = ["import-untyped"]
# strict = true

[[tool.mypy.overrides]]
module = [
    "matplotlib.*",
    "rasterio.*",
    "pyproj.*",
]
ignore_missing_imports = true
follow_untyped_imports = true

[tool.ruff.lint]
# select = ["ALL"]  # Goal :)
extend-select = [  # Extend the default ruleset:
    "B",
    "F",
    "PTH",
    "T20",  # flake8-print <https://docs.astral.sh/ruff/rules/#flake8-print-t20>
]

[tool.ruff.lint.per-file-ignores]
# Allow print() statements in modules where they are expected
"src/transformez/htdp.py" = ["T201"]
"src/transformez/__init__.py" = ["PTH"]
"tests/**/*.py" = ["T201", "PTH"]

[[tool.mypy.overrides]]
module = [
    "scipy.*",
    "fetchez.*",
]
follow_untyped_imports = false
ignore_missing_imports = true

[tool.pytest.ini_options]
addopts = "-m 'not validation'"
markers = [
    "health: tests requiring live network access to external APIs",
    "validation: scientific validation against external reference systems",
    "slow: tests that are too expensive for the normal unit-test suite",
    "vdatum: tests requiring the locally installed NOAA VDatum Java engine",
]
filterwarnings = [
    "ignore::pydantic.warnings.PydanticDeprecatedSince20",
    "ignore:.*Support for class-based `config` is deprecated.*",
    "ignore::DeprecationWarning:planetary_computer.*"
]
